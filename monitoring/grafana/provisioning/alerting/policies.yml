apiVersion: 1

# Roteia todos os alertas pro contact point do webhook.
policies:
  - orgId: 1
    receiver: neura-webhook
    group_by: ['grafana_folder', 'alertname']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
